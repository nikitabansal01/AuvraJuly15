-r requirements.txt

# Test and static-analysis tooling is intentionally excluded from the runtime
# container. Versions are direct inputs; requirements-dev.lock freezes the
# complete dependency graph with hashes for reproducible CI installs.
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
isort==5.12.0
flake8==6.1.0
mypy==1.7.1
