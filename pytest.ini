[pytest]
# Only collect the real unit/integration tests.
# This prevents script-style files in the repo (e.g. under scripts/) from being
# imported during collection.
testpaths = tests

# Allow `async def test_*` without requiring `@pytest.mark.asyncio`.
asyncio_mode = auto
