# Verification tooling for the v2 service only.  This intentionally does not
# pull in the archived legacy application dependency graph.
-r requirements-v2.txt

pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
pip-audit==2.9.0
cyclonedx-bom==7.2.1
